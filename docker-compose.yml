version: '3.8'

services:
  neo-writer:
    build: .
    container_name: neo-writer
    ports:
      - "3007:3000"
    environment:
      - PORT=3000
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped