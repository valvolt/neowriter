services:
  neo-writer:
    build: .
    container_name: neo-writer
    ports:
      - "3007:3000"
    environment:
      - PORT=3000
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped